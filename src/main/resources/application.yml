server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/isukach/spring-cloud-config-file
          skipSslValidation: true
          clone-on-start: true
